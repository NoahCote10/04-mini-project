const span = document.getElementById("word")
// const word ["javascript", "array", "variable"]

// const word
// const secretWord += "_";

// word.textcontent = secretWord;

document.addEventListener("keydown", function(e) {
    console.log();
    // e.key === word[i]
    // secretWord[i] = e.key;
    // word.textContent = secretWord;
})